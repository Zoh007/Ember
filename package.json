{
  "name": "recall",
  "version": "0.1.0",
  "description": "A private desktop memory assistant that captures work context locally and generates daily briefings.",
  "main": "app/electron/main.js",
  "scripts": {
    "start": "electron .",
    "build:python": "node scripts/build-python.js",
    "package": "npm run build:python && electron-builder --dir",
    "package:installer": "npm run build:python && electron-builder --publish=never",
    "release": "npm run build:python && electron-builder --publish=always",
    "briefing": "python3 -m recall_ai.cli briefing",
    "init-db": "python3 -m recall_ai.cli init-db",
    "test": "python -m unittest discover -s tests",
    "check:package:sqlcipher": "node scripts/check_package_sqlcipher.js"
  },
  "keywords": [
    "desktop",
    "memory",
    "electron",
    "sqlite",
    "ai"
  ],
  "author": "Ember <hello@example.com>",
  "license": "UNLICENSED",
  "private": true,
  "dependencies": {
    "@tesseract.js-data/eng": "latest",
    "tesseract.js": "latest"
  },
  "devDependencies": {
    "electron": "^27.0.0",
    "electron-builder": "latest",
    "electron-rebuild": "^2.3.5"
  },
  "build": {
    "appId": "com.ember.desktop",
    "productName": "Ember",
    "asar": false,
    "files": [
      "app/electron/**/*",
      "package.json",
      "node_modules/**/*"
    ],
    "extraResources": [
      {
        "from": "dist-python",
        "to": "python"
      }
    ],
    "directories": {
      "output": "release"
    },
    "mac": {
      "category": "public.app-category.productivity",
      "target": [
        "dmg",
        "zip"
      ]
    },
    "win": {
      "target": [
        "nsis",
        "zip"
      ]
    },
    "linux": {
      "category": "Utility",
      "maintainer": "Ember <hello@example.com>",
      "target": [
        "AppImage",
        "deb"
      ]
    }
  }
}
